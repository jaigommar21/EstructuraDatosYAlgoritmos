nros_pila = [30,40,2,15,10]

for i in range(10000000):
    nros_pila.append(18)



if len(nros_pila) :
    atender_pila = nros_pila.pop()

atender_pila = nros_pila.pop()
atender_pila = nros_pila.pop()
atender_pila = nros_pila.pop()
atender_pila = nros_pila.pop()
atender_pila = nros_pila.pop()
atender_pila = nros_pila.pop()
atender_pila = nros_pila.pop()
atender_pila = nros_pila.pop()

print(nros_pila)



